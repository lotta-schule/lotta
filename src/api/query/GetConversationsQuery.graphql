query GetConversationsQuery {
    conversations {
        id
        updatedAt
        groups {
          id
          name
        }
        users {
            id
            name
            nickname
            avatarImageFile { id }
        }
        messages { id }
    }
}

