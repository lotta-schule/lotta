mutation UpdateUser($id: ID!, $groups: [SelectUserGroupInput!]) {
    user: UpdateUser(id: $id, groups: $groups) {
        id
        groups {
            id
        }
        assignedGroups {
            id
        }
    }
}
