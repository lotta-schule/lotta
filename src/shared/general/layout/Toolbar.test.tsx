import * as React from 'react';
import { render } from 'test/util';
import { Toolbar } from './Toolbar';

describe('Toolbar', () => {
    it('should render', () => {
        render(<Toolbar />);
    });
});
