query GetTenantTimeseriesAnalytics($date: Date!, $metric: AnalyticsMetric!, $period: AnalyticsPeriod!) {
  metrics: timeseriesAnalytics(date: $date, metric: $metric, period: $period) {
    date
    value
  }
}
