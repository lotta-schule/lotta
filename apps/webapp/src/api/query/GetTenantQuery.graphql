query GetTenant {
  tenant {
    id
    title
    slug
    host
    identifier
    configuration {
      backgroundImageFile {
        id
      }
      logoImageFile {
        id
      }
      customTheme
      userMaxStorageConfig
    }
  }
}
