query GetTenant {
  tenant {
    id
    title
    slug
    host
    identifier
    backgroundImageFile {
      id
    }
    logoImageFile {
      id
    }
    configuration {
      customTheme
      userMaxStorageConfig
    }
  }
}
