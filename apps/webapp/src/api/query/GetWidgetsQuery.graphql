query widgets {
  widgets {
    id
    title
    type
    configuration
    iconImageFile {
      id
      formats(category: "ICON") {
        name
        url
        type
        availability {
          status
        }
      }
    }
    groups {
      id
      sortKey
      name
    }
  }
}
