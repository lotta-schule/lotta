query widgets {
  widgets {
    id
    title
    type
    configuration
    iconImageFile {
      id
      formats {
        name
        url
        type
        status
      }
    }
    groups {
      id
      sortKey
      name
    }
  }
}
