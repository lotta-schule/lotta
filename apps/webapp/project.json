{
  "name": "webapp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run typecheck",
        "cwd": "apps/webapp"
      }
    }
  }
}
