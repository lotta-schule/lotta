<h1>Kunde bearbeiten</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.customer_path(@conn, :update, @customer)) %>

<span><%= link "Zurück", to: Routes.customer_path(@conn, :index) %></span>
