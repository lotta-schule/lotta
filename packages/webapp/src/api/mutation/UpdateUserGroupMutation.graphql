mutation UpdateUserGroup($id: ID!, $group: UserGroupInput!) {
  group: updateUserGroup(id: $id, group: $group) {
    id
    name
    isAdminGroup
    sortKey
    canReadFullName
    enrollmentTokens
  }
}
