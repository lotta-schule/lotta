mutation UpdateWidget($id: ID!, $widget: WidgetInput!) {
    widget: updateWidget(id: $id, widget: $widget) {
        id
        title
        type
        configuration
        iconImageFile {
            id
        }
        groups {
            id
            sortKey
            name
        }
    }
}
