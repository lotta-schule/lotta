query GetConversationsQuery {
  conversations {
    id
    updatedAt
    unreadMessages
    groups {
      id
      name
    }
    users {
      id
      name
      nickname
      avatarImageFile {
        id
      }
    }
    messages {
      id
    }
  }
}
